Deploy the project to Vercel production. Follow these steps in order:

1. **Stage changes**: Run `git add` for all relevant modified files (never use `git add -A`, be specific).
2. **Commit**: Create a commit with a concise message describing what changed. End with `Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>`.
3. **Push**: Run `git push` to push to the remote.
4. **Deploy**: Run `vercel --prod` to deploy to Vercel production.
5. **Report**: Show the live URL when done.

If any step fails, stop and report the error — do not skip steps.
